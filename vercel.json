{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd expo && npm install",
  "buildCommand": "cd expo && npx expo export --platform web",
  "outputDirectory": "expo/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
